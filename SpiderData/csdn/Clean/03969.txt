提示错误：
 
 这是因为缺少pillow包，解决：
 1.在环境变量Path中新建python安装目录下的Scripts路径
 
 2.打开cmd输入：pip install pillow安装
 
 安装成功后，import PIL就不会报错了
 
 pip常用命令：

bundle，创建包含多个包的pybundles；
freeze，显示所有已安装的包；
help，显示可用命令；
install，安装包；
search，搜索PyPi；
uninstall，卸载包；
unzip，解压缩单个包；
zip，压缩单个包。

