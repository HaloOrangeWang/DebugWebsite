
python报错及解决办法汇总

matplotlib

'gbk' codec can't decode byte 0x9a in position 37: illegal multibyte sequence

'gbk' codec can't decode byte 0x9a in position 37: illegal multibyte sequence


matplotlib


‘gbk’ codec can’t decode byte 0x9a in position 37: illegal multibyte sequence

解决办法：用VS-code直接写这个文件，剪切到配置文件路径，然后重新运行就不报错了
 
 
 猜测是因为txt文件的编码是gbk，我又不太会改，只能说还是尽可能直接写吧

