python 的异常捕获通常用 try…except…结构

把可能发生错误的语句放在 try 模块里，用 except 来处理异常，会让系统不会直接报错，而是给你具体另一种选项吧。

举个例子 首先将try注释并执行

然后我在输入数字：你好

正常情况下，系统就会自动报错， 提示传入无效的参数

而当我使用try异常捕获时  他就会在错误时给你另一个结果 提示你请输入正确的数字

注意代码格式

如果需要指定异常类型

ZeroDivisionError 是 除(或取模)零 (所有数据类型)

ValueError 是传入无效的参数

另外 在函数使用时 还可以使用raise来接收错误

如以下示例 用raise 主动抛出”密码长度不够“这个异常

同时 还可使用 异常的传递：

输出结果为

