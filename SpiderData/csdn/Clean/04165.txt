这两天学习了Python的if循环语句，但是只要一输入else就会犯错误，非常苦恼，后来发现了错误。原来是因为else缩进不对。下面是我经历的几种错误：

第一种：输入完 print(chTest) 之后用backspace删除，使else和if"表面"上的缩进一致。出现下面的错误：
 
 第二种：输入完 print(chTest) 之后直接 enter，然后直接输入else，出现下面的错误：
 
 第三种：输入完 print(chTest) 之后直接 enter，然后直接输入else以及后面的内容，出现下面的错误：
 
 经历了这么多次试错，我终于发现了端倪，(我这愚笨的脑子。。。。。)：else缩进的正确位置应该在最左边。个人是这样理解的：if那行前面虽然有三个>>>，但这属于python默认的格式，而我们输入if的时候并没有缩进，所以我们在输入else的时候也不用缩进，直接顶格写就好。
 `
 我觉得不会有人犯我这种低级错误，哈哈哈，这篇就当记录吧。

