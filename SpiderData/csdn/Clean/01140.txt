
python 更新pip报错 解决方法大全

在使用Python的pip安装第三方库时会提示当前pip版本低，要求更新，但更新时会报错，请依次尝试以下几种方法：


在使用Python的pip安装第三方库时会提示当前pip版本低，要求更新，但更新时会报错，请依次尝试以下几种方法：

1、python -m pip install --upgrade pip
 2、python -m pip install -U --force-reinstall -i https://pypi.doubanio.com/simple/ pip
 3、python -m pip install -U --force-reinstall pip

