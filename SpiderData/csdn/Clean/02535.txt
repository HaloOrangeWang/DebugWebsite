方法：跳过未知错误重新运行主程序，其实算不上真正意义上的重启，只适合程序偶尔异常跳过异常，方法简单有效。

def python_program(): #将主程序放在此函数下
    print('1')
    b = float(dsd)

if __name__ == '__main__':
    while True :
        try:
            python_program()
        except:
            continue

