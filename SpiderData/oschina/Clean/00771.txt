报错：
 You are using pip version 10.0.1, however version 18.0 is available.
 You should consider upgrading via the 'python -m pip install --upgrade pip' command.

Win+R 输入 cmd 回车，运行了 python -m pip install --upgrade pip 即可。

如果一定要用 adaconda prompt 运行，需要用 管理员身份运行。
 Win + 找到 adaconda prompt 右键 -> 更多 -> 以管理员身份运行
 就会看到如图

再次运行那命令： python -m pip install --upgrade pip
 就会成功了。
 如果还不行，你是不是没交网费？

