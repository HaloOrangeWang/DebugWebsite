最近在做邮件发送的服务，根据任务调度定时发送。问题是：服务在本地跑的时候都可以发送成功，放到服务器上之后发现一只发送失败，日志记录报错，错误如上。

寻找解决该问题的方法时，发现主要有：

1.hostName写错

2.邮箱没有开启服务验证

3.授权密码失效

由于本地测试成功并且使用的是公司服务，还有其他邮件服务正常运行，所以上面3条在这里不适用。

当我 ping 服务器与163服务域名时，发现是失败的。

【ping smtp.163.com

ping: unknown host smtp.163.com】

解决方法：在服务器host文件中，添加邮箱的DNS服务即可。

