尝试了某度常见的三种方法（利用cmd安装、降低pip版本、修改pycharm_tools文件）后仍报错，后经查验，发现手册上所列依赖包lxml没有安装，尝试了一下先安装lxml包，最终装上了docx，可以进行下一步操作啦。

这不是我第一次在使用pycharm安装包的过程中遇到pip version的报错，之前通过修改pycharm_tools和pip version解决了问题，但是这一次虽然报了pip的error，最终并不是它的问题，特此记录。

