
运行项目时报错：


解决不成功案例：

搜了好多文章，只有一个把npm改成cnpm的成功了。


成功案例：

npm cache clean -f

npm install -g cnpm --registry=https://registry.npm.taobao.org

cnpm install -g n（npm时一直报错）

cnpm install -g n --force

但是这种n stable也不成功

但是项目之中已经可以npm i了。

希望大佬指点！

