启动项目的时候报错端口被占用：

拿到被占用的端口号：

打开CMD执行命令

netstat -ano|findstr "8080”

拿到PID "3756";

再执行命令

tasklist|findstr "3756"

查看此PID对应的进程名称为软件名称：

可以看到对应的软件名称为：“WeChat.exe”,可以打开任务管理器关闭对应的程序或者使用命令杀掉进程即可。

也有经常莫名其妙的找不到占用端口号的进程，那么可以试一下以下命令：

taskkill /F /IM java.exe

可以将对应的占用端口号的进程杀死。

