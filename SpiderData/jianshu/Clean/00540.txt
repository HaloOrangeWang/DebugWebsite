nodejs报错require() of ES Module XXX not supported。一般情况下是因为当前使用依赖的版本过高，需要适当降低版本。

nodejs报错require() of ES Module XXX not supported。一般情况下是因为当前使用依赖的版本过高，需要适当降低版本。

卸载当前依赖包

卸载当前依赖包

npm uninstall XXX

安装指定版本的依赖包

npm i xxx@版本号

再次重新运行，问题解决

