没有装lxml模块，不要慌，装上就好啦


1. 应该是没有安装 lxml

pip install lxml


报错：

pip: command not found pip

那就是你还没有装pip


装上之后运行你的py文件就可以啦

如果还是遇到其他ModuleNotFoundError问题，按照对应问题装上对应模块就可以啦

比如

pip install lwxt
pip install xml
.......

