
1. 提示的错误代码


2. 提供的数据


1. 一个字典


2. 具体错误的地方是这段文字


3. 错误的代码


3. 错误分析

符号不匹配，看下面的sql语句

python的sqllite库似乎只是对语句简单的拼接，所以我使用 单引号 的时候，只要文本中出现单引号就会造成不匹配的情况

解决办法

替换单引号，变成两个单引号，类似于c语言中printf中让其显示%的做法，即：输入 %% 就可以显示 % 了


4. 解决问题后的代码

在这里我使用re.sub进行替换

类似于c语言中printf中让其显示%的做法，即：输入 %% 就可以显示 % 了。所以哪怕我们把单引号替换成了两个单引号，但是最后数据库的结果还是和原数据一样。

因为我的sql语句是用单引号的，所以我的数据中不能出现单引号。如果你的sql语句是使用双引号，那么你的数据中就不能出现双引号（我没试过，猜测应该是这样的）

