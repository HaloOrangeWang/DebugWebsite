环境：DC+Exchange 2013（前后端）   
故障：

1.登录ECP、OWA，出现无法显示页面报错   
2.EMS无法连接到服务器，报错

排除方法：

查看服务状态是正常的，查看事件日志，发现一大堆httpevent 15021的报错，查看IIS的虚拟目录，发现IIS的default虚拟目录443没绑定证书、Exchange Back End虚拟目录页没绑定证书。

解决方案：

在IIS管理器，编辑绑定虚拟目录，绑定证书即可。

