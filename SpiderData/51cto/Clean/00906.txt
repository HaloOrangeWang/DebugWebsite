python -m pip install --upgrade pip

在用python做分词、数据处理的时候，想调用pynlpir库，pynlpir.open()时出现错误，更新一下授权文件还是错误。

可是，pip不能用了，

原因是pip中配置的安装库的路径也变了

然后，更新了一下pip，cmd中输入命令：python -m pip install --upgrade pip

然后就可以了

