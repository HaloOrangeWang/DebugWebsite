在家使用Mac电脑测试没问题，把代码放在Windows电脑上却提示：configparser.NoOptionError

（相同一个配置文件，读取数据库信息可以成功，读取url信息失败）

解决方案：

1.遇到这问题，先百度看下是什么原因导致的，有一个解决方案是这样的，但我试了下还是失败：

2.还是不放弃ini文件，发现读取英文没有问题；

3.切换编码格式，修改为GBK就可以了（txt文件也需要是GBK）。

总结：

1.以ini格式的文件，UTF-8时读取中文失败，以GBK读取中文可以成功；

2.而yaml格式的文件，需要是UTF-8。

END~

