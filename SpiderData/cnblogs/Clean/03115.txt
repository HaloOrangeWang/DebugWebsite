
方法/步骤

可以看到的是运行的代码的提示为找不到为selenium文件。

可以看到的是运行的代码的提示为找不到为selenium文件。

首先的需要从官网进行下载selenium的文件。

首先的需要从官网进行下载selenium的文件。

下载完成之后，把文件进行放入python安装路的lib/site-packages文件夹下。

下载完成之后，把文件进行放入python安装路的lib/site-packages文件夹下。

然后使用cmd的进行cd /进入到c盘当中。

然后使用cmd的进行cd /进入到c盘当中。

进入到了c盘之后，切换site-packages当前文件下。

进入到了c盘之后，切换site-packages当前文件下。

可以看到的是当前的切换到selenium-3.11.0的文件下。

可以看到的是当前的切换到selenium-3.11.0的文件下。

进入到了的文件下之后，进行执行命令为 python setup.py install来。

进入到了的文件下之后，进行执行命令为 python setup.py install来。

等待命令执行完成之后没有报错，说明提示安装成功了。

等待命令执行完成之后没有报错，说明提示安装成功了。

然后执行的selenium相关代码之后就没有报错了。

然后执行的selenium相关代码之后就没有报错了。

