1、今天刚试了下httprunner运行了用例，用的是get方法，运行中遇到了如下的问题，运行的返回结果是正确的，就是多了一处exception异常错误：

2、上图报错，异常验证失败，断言错误：ValueActudio失败，上百度查了下没查到结果，最后突然想起来，在python中unittest中调用测试用例，用例的名称是必须以test开头的，

我之前用的用例为汉字开头，如图中的第2条，后面我把名字改成了英文test开头，OK成功解决，没有再报第一点中的错误了。小小的注意点还请大家注意，嘿嘿

3、单独运行第一个以test开头的用例，执行结果如下：

在python中的unittest中集成测试用例时，测试用例的函数是必须要以test开头的，否则就会报错，这边httprunner也必须是要和python中的unittest中一致，是不是小小的一个小问题就能解决，我也头疼不爱动脑，随机想到~

不好意思，上面的方法错了，我后面又出现了，一直研究是发现了我没有设置vrildate才会成功，设置了一样会失败，后面一直找原因原来是设置的类型错了，比如content.status应该选择int，我选的是string，对应类型就OK了

