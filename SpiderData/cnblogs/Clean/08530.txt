RecursionError:maximun recursion depth exceeded
超过了最大递归深度


RecursionError:maximun recursion depth exceeded

原因：
在使用@property装饰器时，方法名与实例的属性同名，在赋值进从而产生了无限递归


原因：

在使用@property装饰器时，方法名与实例的属性同名，在赋值进从而产生了无限递归

来自为知笔记(Wiz)

