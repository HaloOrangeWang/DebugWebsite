今自定义模块后非相同目录导出提示找不到模块报错信息如下：

各方查找各位大神方法很多

参考链接

1、在需要导入的文件夹创建 __init.py__ #这个文件夹下记得每次创建，但此次问题尝试后还是无效

2、将文件放到python安装目录下的 lib #文件太多没尝试此方法

3、import sys

sys.path.append(’引用模块的路径’)

最终解决办法如下：

右击项目根目录将目录标记为根源，使py查找你要导入的模块时从此处开始查找

以上方法是在PyCharm 中创建了__init.py__还是报错的解决办法笔记

