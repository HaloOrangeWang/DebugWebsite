
问题

在VSCode中，选执行命令vue --version 等命令时，产生如下报错：


解决方案

在Windows应用中找到Windows PowerShell，以管理员运行：

在Windows应用中找到Windows PowerShell，以管理员运行：

在命令框输入： set-ExecutionPolicy RemoteSigned， 然后输入A即可解决

在命令框输入： set-ExecutionPolicy RemoteSigned， 然后输入A即可解决

