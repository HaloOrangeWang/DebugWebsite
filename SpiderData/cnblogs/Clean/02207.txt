用pycharm写python的时候，import request时，报错：ModuleNotFoundError: No module named request

emmmm。解决方法：pip install request。安装成功request了，报错就消失了。

命令提示符（cmd）- pip install request。

备注：首先得有pip。没有的话就安装吧。（我安装的python是3.6的）

