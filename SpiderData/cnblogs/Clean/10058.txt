
先说一下背景

最近我接手师兄的一个 ui 项目，刚开始因为库、python两者的版本不同而报错，故采用了让师兄直接发我虚拟环境的库文件我解压导入即可。见 python快速安装大量库，想要运行别人的大型项目代码？直接让他把库打包发过来 ，

这样而来，代码可以运行了，而后又出现了新的问题，即再配置 QtDesigner 时提示This application failed to start because no Qt platform plugin could be initialized，由于不是直接pip安装的，所以很多文档不起作用，找了很多资料才搞定


真传一句话，假传万卷书

刚直接复制过来的是这样的

然后替换文件，就在上一级目录

