
注:该文章仅供作者以及他人参考，也许内容并不靠谱

解释器提示如：

但又一直找不到问题点的话，请确保代码行内没有夹杂中文的空格，tab等，非文字字符．

报错：DataFrame没有该属性：reshape

一直找不到问题点的话，请确保代码行内没有夹杂中文的空格，tab等，非文字字符．

对象模块不可调用：

算数运算报错

参数位置不正确：(可能由于python参数顺序不正确)

python代码错误，字符错误

符号错误：

出现错误是因为在相乘时将数据转为了int类型，数字带小数部分转为int相乘时就会报错的
 最好是转为float类型

漏掉了符号

在将string转为float时出现错误：
 自己查一查字符串哪里多出来了/t或者空格等

类型错误：元组索引必须是整数或切片，而不是字符串。

使用pandas的join时报错：
 是因为合并的两个表的列名不符合“一致”的要求

读取的文件名中有中文会报错：

翻译为：非法语句。
 说明代码中英文标点少了

不是有效的关键字参数名称。关键字参数必须是有效的标识符：

空格或缩进错误：

语法错误：
 多了或者少了东西

