1、如果python安装在C盘的话，那么在通过pip安装模块时需要以管理员形式打开命令窗口去执行pip 安装命令

　　操作方法：按win+X 键，选择windows powershell （管理员A)菜单并打开（打开是蓝色窗口），然后在命令窗口输入pip install xx或者pip install --upgrade xxx就不会报错

2、如果python安装在非C盘（例如D盘），那么可以直接通过普通的命令窗口（打开是黑色窗口）去执行pip安装或者升级命令且不会报错，你get到了没，哈哈。

