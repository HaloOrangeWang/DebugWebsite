ERROR: Command “python setup.py egg_info” failed with error code 1 in /private/tmp/pip-install-yC_D4c/MySQL-python/

ERROR: Command “python setup.py egg_info” failed with error code 1 in /private/tmp/pip-install-yC_D4c/MySQL-python/

在终端执行下面命令：  brew install mysql

在终端执行下面命令：

在此执行pip install MySQL-python， 在此报错。

在此执行pip install MySQL-python， 在此报错。

执行下面3条命令。  brew unlink mysql
 brew install mysql-connector-c
 brew link --overwrite mysql

执行下面3条命令。

再此pip install MySQL-python。

再此pip install MySQL-python。

