1. 在pycharm 中安装python包会报错“pycharm ModuleNotFoundError: No module named 'distutils.core'”；

2. 可能原因：inux 下的默认的python 版本是2.7  这也就导致了在pycharm 中没有办法使用pip；

3. 解决方案： 终端下执行指令“sudo apt-get install python3-pip”;

4. 然后再 pycharm 中就可以只用 install，安装python包了。

