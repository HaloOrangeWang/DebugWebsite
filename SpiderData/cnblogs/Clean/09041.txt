很明显，是缺少所需要的环境。Google了一下，到网站上下载与自己python和电脑相适应的包，然后在cmd里使用pip install 安装。。

到这里下载：https://www.lfd.uci.edu/~gohlke/pythonlibs/

ctrl + F 找到自己需要的包，具体信息如下：

下载好后，使用pip安装这个包：

pip install 上面下载文件的全路径

或者直接在下载位置打开cmd也行。

之后可以到Python-》Lib-》site-packages看一下。

回到pycharm，再次尝试导入：

成功了，到这就行了。如果还是失败，


PyCharm无法找到已安装的Python类库，就再进行一步

这样就可以！！！！

