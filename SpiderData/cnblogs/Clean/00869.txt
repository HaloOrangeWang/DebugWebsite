写kNN，需要在python中实现kd-tree

思考了一下，在python下写这种算法类的东西，还是十分别扭

于是希望用ctypes调用一下c++动态加载库

于是尝试实现一下

于是报错

最后百度发现原因是c++的编译后，函数名会被改变（为了实现重载）

用extern "C"声明后，就会使用c的方式进行编译，编译后的文件中仍然是定义的函数名

解决方法

改后缀为.c

声明为"C", 以c方式编译即可

