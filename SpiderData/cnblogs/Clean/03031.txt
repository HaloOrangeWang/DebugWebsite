
1 expected an indented block

缩进的问题，像if 后面一行要缩进一样。


2 TypeError: 'NoneType' object is not callable

None类型不是一个可以调用（callable）对象。赋值语句错误，值为None。callable对象是指一个后面可以加（）的对象，一般把调用函数时（）去掉。


3 ImportError:DLL load failed 找不到指定模块

可能是没安装完整的问题，使用 pip  uninstall  XXX 重新安装。

